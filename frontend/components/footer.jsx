import { withRouter, Link } from 'react-router-dom';
import React from 'react';

const Footer = (props, ownProps) => {
  // page = ownProps.location.pathname.slice(1);
  // if (page === 'login') {
    return (
      <footer className="footer-splash">
    
      </footer>
    )
}

export default withRouter(Footer);
